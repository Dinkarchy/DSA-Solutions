#include <bits/stdc++.h>
using namespace std;

int main() {
    int T; cin >> T;
    while (T--) {
        int X, Y; cin >> X >> Y;
        if (X < Y) cout << "BIKE\n";
        else if (Y < X) cout << "CAR\n";
        else cout << "SAME\n";
    }
    return 0;
}
